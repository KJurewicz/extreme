def find_maxima(arr):
	""" Returns the position of the maximum values """
	for ind in arr:
		index = 0
		for i in range(1,len(arr)):
			if arr[i] > arr[index]
				index = i 
		print index
	return index